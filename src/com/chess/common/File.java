package com.chess.common;

public enum File {
    /* File = Column of the piece
    // n enum is a special "class" that represents a group of constants
    // (unchangeable variables, like final variables). */
    A,B,C,D,E,F,G,H
}
